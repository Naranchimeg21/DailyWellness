import {AppActionTypes, AppInitialState, AppInitialType} from './app.types';

interface ProviderPropsType {
  children: React.ReactNode;
}

export {AppActionTypes, AppInitialState};
export type {AppInitialType, ProviderPropsType};
